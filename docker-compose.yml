services:
  app:
    image: node:18
    command: sh -c "mkdir -p /app/logs && chmod -R 777 /app/logs && npm install && npm start"
    working_dir: /app
    volumes:
      - ./src:/app
      - ./logs:/app/logs
    ports:
      - "3000:3000"
  prometheus:
    image: prom/prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
  grafana:
    image: grafana/grafana
    ports:
      - "3001:3000"
    depends_on:
      - prometheus
  loki:
    image: grafana/loki
    ports:
      - "3100:3100"
  promtail:
    image: grafana/promtail
    volumes:
      - ./promtail-config.yml:/etc/promtail/config.yml
      - ./logs:/app/logs
    depends_on:
      - loki
